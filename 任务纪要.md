rag设计思路：

步骤1        步骤2        步骤3        步骤4        步骤5        步骤6
PDF解析  →  图片存储  →  AI描述   →  向量嵌入  →  关联构建  →  检索问答

步骤 3
┌──────────┐     ┌───────────────┐     ┌──────────────┐
│  图片    │────▶│  视觉大模型   │────▶│  结构化描述  │
│  (原图)  │     │  (GLM-4V)    │     │              │
└──────────┘     └───────────────┘     └──────────────┘

内容	嵌入文本	向量用途
文本块	原文	文本语义检索
图片描述	AI生成的描述文本	图片语义检索
图注	提取的图注文本	图片关键词检索

集合1: text_chunks（文本块）
  ├── chunk_id
  ├── document_id
  ├── text
  ├── embedding (2048维, 智谱embedding-3)
  └── metadata {page_num, section}

集合2: image_descriptions（图片描述）
  ├── image_id
  ├── document_id
  ├── description
  ├── embedding (2048维)
  ├── image_url (MinIO路径)
  └── metadata {page_num, type, keywords}