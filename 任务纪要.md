## 维护一份项目任务纪要文档，这份文档将作为未来面试的展示材料
1. **持续更新**：之后，我每完成一个新的功能，都会向你描述这个功能。你需要立刻将该功能的相关信息追加到任务纪要中，保持文档的连续性和完整性。
2. **记录内容要求**：针对每一个功能，必须包含以下三部分，且**不能出现任何代码**：
   - **技术实现**：概括该功能用到的技术方案、架构设计、核心逻辑或关键流程，用自然语言描述。
   - **遇到的问题**：列出开发过程中遇到的主要困难、阻塞点或意外情况。
   - **解决方案**：清晰说明最终是如何攻克上述问题的，包括思路、工具、方法或妥协方案。




# 材料学院智能学习平台 - 任务纪要

> **文档用途**：记录项目开发过程中的技术实现、问题解决、学习收获，作为面试材料
>
> **创建日期**：2026-05-19
>
> **最后更新**：2026-06-02（新增"一键启动脚本开发"章节）

---

## 📚 目录

- [项目概述](#项目概述)
- [技术选型](#技术选型)
- [已完成功能](#已完成功能)
  - [1. 项目初始化与基础设施搭建](#1-项目初始化与基础设施搭建)
  - [2. 智谱API集成与测试](#2-智谱api集成与测试)
  - [3. PDF解析服务开发](#3-pdf解析服务开发)
  - [4. KnowledgeGraph项目改造](#4-knowledgegraph项目改造)

---

## 项目概述

### 项目背景

材料学院智能学习平台是一个基于 AI Agent 的智能问答系统，整合材料学科知识库，为学生提供智能问答、图片检索、PPT自动生成等功能。

### 核心功能

| 功能模块 | 描述                 | 优先级 |
| -------- | -------------------- | ------ |
| 智能问答 | 基于知识库的RAG问答  | P0     |
| 图片检索 | 教材配图关联检索     | P0     |
| 知识图谱 | 材料学科知识关联导航 | P1     |
| PPT生成  | 自动生成学习材料     | P1     |

### 技术架构

系统采用分层架构设计：

- **前端层**：React + Vite + TypeScript，提供用户交互界面
- **后端API层**：FastAPI 异步框架，处理业务逻辑
- **Agent智能层**：LangChain/GraphRAG + 智谱 AI，实现知识抽取和问答
- **数据存储层**：PostgreSQL + Milvus + Redis + MinIO

---

## 技术选型

### 后端技术选型

| 技术           | 选型理由                                            |
| -------------- | --------------------------------------------------- |
| **FastAPI**    | 异步支持好、自动生成API文档、类型提示完善、性能优异 |
| **PostgreSQL** | 成熟稳定、支持JSON类型、社区活跃                    |
| **Milvus**     | 开源向量数据库、支持多种索引类型、性能优秀          |
| **Redis**      | 高性能缓存、支持多种数据结构、应用广泛              |

### AI 技术选型

| 技术                 | 选型理由                                  |
| -------------------- | ----------------------------------------- |
| **智谱 GLM-4-flash** | 国产大模型、性价比高、中文能力强、API稳定 |
| **智谱 embedding-3** | 2048维向量、语义表示能力强、与GLM系列配套 |
| **GraphRAG**         | 微软开源、自动构建知识图谱、支持增量更新  |

### 前端技术选型

| 技术             | 选型理由                             |
| ---------------- | ------------------------------------ |
| **React 18**     | 生态成熟、组件化开发、虚拟DOM性能好  |
| **TypeScript**   | 类型安全、开发体验好、减少运行时错误 |
| **Vite**         | 构建速度快、热更新体验好、配置简单   |
| **Tailwind CSS** | 原子化CSS、开发效率高、文件体积小    |

---

## 已完成功能

---

## 1. 项目初始化与基础设施搭建

### 1.1 功能描述

搭建项目的开发环境、版本控制、容器化基础设施，为后续开发提供稳定的基础服务。

### 1.2 技术实现

#### 项目结构设计

采用前后端分离的项目结构，按功能模块划分目录：
- **backend**：后端服务，采用分层架构（API层、服务层、数据层）
- **frontend**：前端服务，采用组件化开发
- **docker**：Docker 配置文件，统一管理容器服务
- **docs**：项目文档
- **data**：数据文件
- **KnowledgeGraph**：知识图谱构建模块

#### Docker 服务部署

使用 Docker Compose 统一管理所有基础服务：

| 服务       | 端口      | 作用                                 |
| ---------- | --------- | ------------------------------------ |
| PostgreSQL | 5432      | 关系数据存储（用户数据、文档元信息） |
| Milvus     | 19530     | 向量数据库（知识向量检索）           |
| Redis      | 6379      | 缓存（会话管理、热点数据缓存）       |
| MinIO      | 9000/9001 | 对象存储（图片、PDF文件存储）        |
| etcd       | 2379      | Milvus 元数据存储                    |

#### 环境管理

- 使用 `.env` 文件管理环境变量，避免敏感信息硬编码
- 区分开发环境和生产环境配置
- 使用 Git 分支策略管理版本（main/develop/feature）

### 1.3 遇到的问题

#### 问题1：Docker Desktop 启动缓慢

**现象**：Windows 上 Docker Desktop 启动时间过长，有时超过 5 分钟。

**原因分析**：WSL2 虚拟机默认分配过多内存，导致启动时资源分配缓慢。

**解决方案**：
1. 创建 `.wslconfig` 文件限制 WSL2 资源使用（内存限制为 8GB）
2. 使用 `wsl --shutdown` 命令完全关闭 WSL2 后再启动

**经验总结**：Windows 下使用 Docker 需要注意 WSL2 的资源配置，合理限制资源可以提升启动速度。

#### 问题2：端口冲突

**现象**：启动容器时报错端口已被占用。

**原因分析**：本地其他服务（如 PostgreSQL 本地安装版）占用了相同端口。

**解决方案**：
1. 使用 `netstat -ano | findstr :端口号` 查看端口占用情况
2. 修改 docker-compose.yml 中的端口映射，或停止占用端口的服务

**经验总结**：在 Windows 上开发时，要注意本地安装的服务与 Docker 容器服务的端口冲突问题。

### 1.4 学习收获

1. **Docker 网络理解**：了解了 Docker 的 bridge 网络模式，容器间通过服务名互相访问，无需知道具体IP。
2. **数据持久化**：理解了 Docker Volume 的作用，确保容器重启后数据不丢失。
3. **环境隔离**：掌握了使用环境变量和 .env 文件管理不同环境的配置。

---

## 2. 智谱API集成与测试

### 2.1 功能描述

集成智谱 AI 的 GLM-4-flash 大语言模型和 embedding-3 向量化模型，为后续的问答和知识图谱构建提供 LLM 能力。

### 2.2 技术实现

#### API 服务封装

设计了一个统一的服务封装层，主要功能包括：
- **对话接口**：支持单轮对话和多轮对话，支持系统提示词
- **向量化接口**：支持单文本和批量文本向量化
- **异步支持**：将同步的 SDK 调用转换为异步调用，适配 FastAPI 的异步架构

#### 关键设计决策

1. **模型选择**：
   - 对话模型选择 GLM-4-flash：性价比高，响应速度快，适合大规模调用
   - Embedding 模型选择 embedding-3：2048维向量，语义表示能力更强

2. **异步处理**：
   - 问题：zhipuai SDK 是同步调用，直接在异步框架中会阻塞事件循环
   - 解决：使用 `asyncio.run_in_executor` 将同步调用放到线程池中执行

3. **API 端点设计**：
   - `/chat`：对话接口，接收用户输入，返回 AI 回复
   - `/embed`：向量化接口，返回文本的向量表示
   - `/batch_embed`：批量向量化接口，支持一次处理多个文本

### 2.3 遇到的问题

#### 问题1：同步SDK与异步框架不兼容

**现象**：直接在 FastAPI 异步端点中调用智谱 SDK，会阻塞整个事件循环，导致其他请求无法处理。

**原因分析**：FastAPI 基于 `asyncio` 事件循环，同步调用会阻塞事件循环，导致并发性能下降。

**解决方案**：
1. 使用 `asyncio.run_in_executor()` 将同步函数放到线程池中执行
2. 使用 `functools.partial` 预填充函数参数
3. 使用默认线程池，无需额外配置

**技术原理**：
- `run_in_executor` 会在单独的线程中执行同步函数
- 主线程的事件循环不会被阻塞，可以继续处理其他请求
- 默认线程池大小为 CPU核心数 * 5，足够应对中小规模并发

#### 问题2：Embedding 维度理解错误

**现象**：最初误以为 embedding-3 输出维度是 1024，与实际不符。

**原因分析**：智谱 AI 的不同 embedding 模型维度不同，没有仔细阅读文档。

**解决方案**：
1. 查阅智谱 API 官方文档，确认模型参数
2. 编写测试代码验证实际输出维度（2048维）
3. 更新 Milvus collection 的向量维度配置

**经验总结**：使用第三方 API 前，务必仔细阅读官方文档，确认参数细节，避免因理解错误导致的返工。

#### 问题3：API Key 安全管理

**现象**：测试时将 API Key 硬编码在代码中，存在安全风险。

**解决方案**：
1. 使用环境变量存储敏感信息
2. 创建 `.env` 文件管理环境变量
3. 将 `.env` 添加到 `.gitignore`，避免提交到版本控制

**最佳实践**：敏感信息（API Key、密码等）应该始终使用环境变量管理，代码中通过 `os.getenv()` 读取。

### 2.4 学习收获

1. **异步编程深入理解**：掌握了 Python asyncio 的工作原理，以及如何在异步框架中集成同步库。
2. **API 设计模式**：学会了服务层封装、依赖注入等设计模式，提高代码可维护性。
3. **向量数据库理解**：理解了 embedding 在语义检索中的作用，向量维度影响检索精度和存储成本。

### 2.5 测试结果

成功测试了智谱 API 的三个核心接口：
- ✅ chat 接口：对话响应正常，返回内容准确
- ✅ embed 接口：向量化正常，输出 2048 维向量
- ✅ batch_embed 接口：批量向量化正常，可一次处理多个文本

---

## 3. PDF解析服务开发

### 3.1 功能描述

开发 PDF 文档解析服务，提取教材中的文本内容、图片、图注等信息，为知识库构建提供数据源。

### 3.2 技术实现

#### 技术选型：PyMuPDF

选择 PyMuPDF（fitz）作为 PDF 解析库，原因：
- 性能优秀：C 语言实现，解析速度快
- 功能全面：支持文本、图片、矢量图提取
- 跨平台：支持 Windows、Linux、macOS
- 开源免费：MIT 许可证

#### 核心功能模块

1. **文本提取**：
   - 提取指定页面的纯文本
   - 支持获取文本的位置信息（坐标）
   - 支持按文本块提取，保留结构信息

2. **图片提取**：
   - 提取嵌入的位图图片
   - 获取图片的尺寸、格式、颜色空间信息
   - 支持导出为 PNG、JPEG 等格式

3. **图注识别**：
   - 基于正则表达式匹配图注模式（如"图1-1"、"Fig.1"）
   - 根据图片位置查找对应的图注文本
   - 支持中英文图注格式

4. **页面渲染**：
   - 将 PDF 页面渲染为图片
   - 支持指定缩放比例（提高清晰度）
   - 支持添加文本高亮

### 3.3 遇到的问题

#### 问题1：PDF 中文乱码

**现象**：某些 PDF 提取的中文文本显示为乱码或方框。

**原因分析**：
1. PDF 内嵌字体没有正确映射到 Unicode
2. 使用了非标准编码的中文字体
3. PDF 使用了矢量绘制文字，而非嵌入字体

**解决方案**：
1. 方案一：使用 `get_text("dict")` 获取更详细的文本信息，包括字体信息
2. 方案二：将页面渲染为图片后，使用 OCR（PaddleOCR/Tesseract）识别文字
3. 方案三：尝试其他 PDF 解析库（pdfplumber、pdfminer）

**最佳实践**：对于字体编码问题的 PDF，渲染 + OCR 是最可靠的方法，但速度较慢。

#### 问题2：图片提取不完整

**现象**：某些 PDF 中的图片无法提取，或提取出来的图片是空白的。

**原因分析**：
1. 图片使用了特殊编码（如 JPEG2000）
2. 图片是通过矢量绘制的，不是嵌入的位图
3. 图片使用了遮罩或引用（SMask），需要额外处理

**解决方案**：
1. 检测图片类型，针对不同类型使用不同的提取方法
2. 对于矢量绘制的"图片"，可以渲染为位图后再保存
3. 使用 PyMuPDF 的 `extract_image` 方法时，检查返回的颜色空间信息

**经验总结**：PDF 图片提取需要考虑多种情况，不能假设所有图片都是嵌入的位图。

#### 问题3：内存占用过高

**现象**：处理大型 PDF（100+ 页）时内存占用超过 2GB。

**原因分析**：一次性将所有图片数据加载到内存中，没有及时释放。

**解决方案**：
1. 使用生成器模式，逐页处理，处理完立即释放内存
2. 不在内存中保存图片数据，直接写入文件或上传到对象存储
3. 使用流式处理，避免一次性加载整个 PDF

**最佳实践**：处理大文件时，应该使用流式处理或分批处理，避免内存溢出。

### 3.4 学习收获

1. **PDF 文档结构理解**：了解了 PDF 的页面、块、行、span 层级结构，有助于理解文本提取的原理。
2. **正则表达式应用**：掌握了复杂的文本模式匹配技巧，用于识别图注格式。
3. **内存优化**：学会了使用生成器和惰性求值处理大数据，避免内存溢出。
4. **OCR 备选方案**：了解了 PDF 解析的限制，知道在什么情况下需要使用 OCR。

### 3.5 服务接口设计

提供了 RESTful API 接口：
- **POST /parse**：上传 PDF 文件，返回解析结果
- **参数**：extract_images（是否提取图片）、extract_captions（是否提取图注）
- **返回**：页面文本、图片列表、图注列表

---

## 4. KnowledgeGraph项目改造

### 4.1 功能描述

复用已有的 KnowledgeGraph 项目（基于 Microsoft GraphRAG），将其改造为使用智谱 AI 服务，构建材料学科知识图谱。

### 4.2 技术实现

#### GraphRAG 架构理解

GraphRAG 的处理流程：
1. **文档加载**：读取输入文件（支持 txt、md、csv 等）
2. **文本分块**：将长文本分割为适合 LLM 处理的块
3. **实体提取**：使用 LLM 从文本中提取实体（材料、工艺、性质等）
4. **关系提取**：使用 LLM 提取实体之间的关系
5. **向量化**：将实体和关系描述转换为向量
6. **社区检测**：使用 Leiden 算法发现实体社区
7. **生成社区摘要**：为每个社区生成描述

#### 配置文件改造

GraphRAG 3.0.9 版本采用了新的配置格式，需要适配：

| 配置项              | 说明                       |
| ------------------- | -------------------------- |
| `completion_models` | LLM 配置块（新格式）       |
| `embedding_models`  | Embedding 配置块（新格式） |
| `api_base`          | 智谱 API 端点              |
| `entity_types`      | 材料领域实体类型定义       |

#### 材料领域实体类型定制

根据材料学科特点，定义了 7 种实体类型：

| 实体类型    | 说明      | 示例                   |
| ----------- | --------- | ---------------------- |
| material    | 材料      | 聚酰亚胺、环氧树脂     |
| process     | 工艺/方法 | 溶胶-凝胶法、热压成型  |
| structure   | 结构      | 分子链结构、晶体结构   |
| property    | 性质      | 热稳定性、介电常数     |
| technology  | 技术      | 纳米复合技术、表面改性 |
| equipment   | 设备      | 扫描电镜、热重分析仪   |
| application | 应用      | 航空航天、电子封装     |

### 4.3 遇到的问题

#### 问题1：GraphRAG 版本配置格式变更

**现象**：使用旧格式配置时报错 `Model ID default_completion_model not found`。

**原因分析**：GraphRAG 3.0.9 版本将配置格式从 `models` 改为 `completion_models` 和 `embedding_models` 两个独立块。

**解决方案**：
1. 查阅 GraphRAG 官方文档，了解新配置格式
2. 更新配置文件，使用新的配置块名称
3. 测试配置是否正确加载

**经验总结**：使用新版本库时，务必查阅最新的官方文档，不要依赖旧教程或示例。

#### 问题2：环境变量解析错误

**现象**：配置文件中环境变量引用报错 `Invalid placeholder in string`。

**原因分析**：GraphRAG 使用 `$` 作为特殊字符，在 `file_pattern` 中需要转义。

**解决方案**：
1. 在 `file_pattern` 中使用 `$$` 转义单个 `$`
2. 创建 `.env` 文件存放环境变量
3. 确保环境变量名称与配置文件中的引用一致

**经验总结**：YAML 配置文件中的特殊字符需要注意转义，避免解析错误。

#### 问题3：缓存类型不支持

**现象**：配置 `cache.type: file` 报错 `CacheConfig.type 'file' is not registered`。

**原因分析**：GraphRAG 3.0.9 只支持 `json`、`memory`、`none` 三种缓存类型。

**解决方案**：将缓存类型改为 `json`，指定缓存存储目录。

#### 问题4：实体提取返回空结果（✅ 已解决）

**现象**：GraphRAG 运行成功但 `extract_graph` 返回 `No entities detected during extraction`。

**分析过程**：

1. **检查输入文件**：文献语料主要是英文，但使用了中文 prompt
2. **测试智谱 API 响应格式**：发现智谱返回格式与 GraphRAG 期望格式不匹配
3. **日志分析**：查看 `logs/indexing-engine.log` 发现 KeyError 错误

**根本原因**：

提示文件 `prompts/extract_graph.txt` 中的示例部分（Example 2 和 Example 3）仍然包含变量占位符 `{tuple_delimiter}`、`{record_delimiter}`、`{completion_delimiter}`，导致 GraphRAG 尝试格式化时出现 `KeyError: 'tuple_delimiter'`。

GraphRAG 在 `graph_extractor.py` 第 87 行尝试格式化 prompt 时，只传递了主指令部分的参数，而没有传递示例部分的参数，导致 KeyError。

**解决方案**：

完全重写提示文件，将所有示例中的变量占位符替换为硬编码的分隔符：
- `{tuple_delimiter}` → `<|>`
- `{record_delimiter}` → `##`
- `{completion_delimiter}` → `<|COMPLETE|>`

修改范围包括：
1. 主指令部分的分隔符说明
2. Example 1、Example 2、Example 3 所有示例中的分隔符
3. 在提示中明确要求："When finished, output <|COMPLETE|> EXACTLY. Do not use any other completion marker."

**验证结果**：

✅ GraphRAG 成功启动并开始处理 33 篇聚酰亚胺研究论文
✅ 生成了 `documents.parquet` 和 `text_units.parquet` 文件
✅ 实体提取流程正常运行，无 KeyError 错误

**经验总结**：

1. **Prompt 模板变量作用域**：在使用模板引擎时，要清楚变量的作用域和替换时机，确保所有需要替换的变量都能正确传递
2. **示例与指令的一致性**：提示文件中的示例必须与指令部分使用完全相同的格式，包括分隔符
3. **调试 LLM 输出格式问题**：当遇到输出格式不匹配时，应该：
   - 先单独测试 API 响应（使用测试脚本）
   - 检查框架的源码了解期望格式
   - 查看 logs 目录下的详细日志
   - 清除缓存后再测试修复

### 4.4 学习收获

1. **GraphRAG 架构理解**：
   - 文档加载 → 分块 → 实体提取 → 关系提取 → 向量化 → 社区检测
   - 每个步骤都可以单独配置和替换
   - 支持增量更新，不需要每次重新处理所有数据

2. **配置驱动开发**：GraphRAG 高度可配置，通过 YAML 文件控制整个流程，无需修改代码。

3. **LLM 输出格式问题**：不同模型厂商的 API 响应格式可能有差异，需要适配：
   - OpenAI 格式是业界标准，但不是所有模型都严格遵循
   - 在选择模型时，需要考虑输出格式的兼容性

4. **调试技巧**：
   - 使用 `--verbose` 参数查看详细日志
   - 单独测试每个组件（先测试 API，再测试 GraphRAG）
   - 查看源码理解错误原因

### 4.5 后续计划

- [x] 解决实体提取格式问题（✅ 已完成 - 重写提示文件，使用硬编码分隔符）
- [x] 测试完整的知识图谱构建流程（⚠️ 部分完成 - 因API速率限制中断）
- [ ] 数据迁移到 Milvus
- [ ] 开发图谱查询 API

---

## 5. 知识图谱构建成果

### 5.1 功能描述

使用 GraphRAG 从 16 篇聚酰亚胺研究论文中自动构建知识图谱，提取材料领域的实体和关系。

### 5.2 技术实现

#### 核心成果

**成功生成的数据**：
- **实体数量**：695 个
- **关系数量**：523 个
- **文本单元**：548 个

**实体类型分布**：

| 实体类型    | 数量 | 占比  | 说明                     |
| ----------- | ---- | ----- | ------------------------ |
| MATERIAL    | 266  | 38.3% | 聚酰亚胺、环氧树脂等     |
| PROPERTY    | 219  | 31.5% | 热稳定性、介电常数等     |
| PROCESS     | 112  | 16.1% | 溶胶-凝胶法、热压成型等  |
| APPLICATION | 38   | 5.5%  | 航空航天、电子封装等     |
| TECHNOLOGY  | 22   | 3.2%  | 纳米复合技术、表面改性等 |
| STRUCTURE   | 14   | 2.0%  | 分子链结构、晶体结构等   |
| EQUIPMENT   | 10   | 1.4%  | 扫描电镜、热重分析仪等   |

#### 技术指标

**API 调用统计**：
- 总请求数：1886 次
- 成功请求：1885 次
- 失败请求：1 次
- 成功率：99.95%
- 缓存命中率：75.4%

**性能数据**：
- 总计算时长：82,056 秒（约 22.8 小时）
- 平均响应时间：43.5 秒/请求
- 总 Token 消耗：4,890,416 个

### 5.3 遇到的问题

#### 问题1：智谱 API 速率限制

**现象**：知识图谱构建过程中遇到 `RateLimitError: 您的账户已达到速率限制` 错误，导致流程中断。

**错误位置**：
- extract_graph 工作流：548/548 ✅ 已完成
- 描述生成阶段：3247/22760 ⚠️ 中断（完成 14.3%）

**原因分析**：
1. GraphRAG 在描述生成阶段需要大量调用 LLM API
2. 智谱 API 有每分钟请求次数限制
3. 当前配置未添加足够的请求间隔

**解决方案**：
1. 修改配置文件，增加重试次数（3 → 5 次）
2. 添加指数退避策略（backoff_factor: 2.0）
3. 添加请求延迟（request_delay: 1.0 秒）
4. 等待 API 速率限制重置后继续运行

**经验总结**：
- 在使用第三方 API 时，需要提前了解速率限制策略
- 对于大规模数据处理，应该预留足够的缓冲时间
- 缓存机制非常重要（75.4% 的缓存命中率节省了大量 API 调用）

#### 问题2：中英文混合内容处理

**现象**：输入的文献主要是英文，但提示词使用了中文。

**解决方案**：
1. 提示词设计为语言无关的格式
2. 实体类型使用英文标签（MATERIAL、PROPERTY 等）
3. GraphRAG 能够正确处理英文内容并生成中文描述

### 5.4 数据质量评估

**实体示例**（前 10 个）：

| 实体名称                                 | 类型        | 描述                                                          |
| ---------------------------------------- | ----------- | ------------------------------------------------------------- |
| FUNCTIONAL POLYIMIDE FILMS               | MATERIAL    | Polyimides are used in MEMS and flexible electronics...       |
| FLEXIBLE MEMS APPLICATIONS               | APPLICATION | Applications of flexible microelectromechanical systems...    |
| MICROFABRICATION                         | PROCESS     | The process of creating small-scale structures or patterns    |
| LASER-INDUCED GRAPHENE                   | TECHNOLOGY  | A technology that uses lasers to create graphene on polyimide |
| PHOTOSENSITIVE POLYIMIDE MICROPATTERNING | TECHNOLOGY  | A technique for creating micro patterns on polyimide...       |

**质量评估**：
- ✅ 实体描述清晰准确
- ✅ 与聚酰亚胺研究领域高度相关
- ✅ 实体类型分类正确
- ✅ 中英文混合处理良好

### 5.5 学习收获

1. **GraphRAG 完整流程理解**：
   - 文档加载 → 文本分块 → 实体提取 → 关系提取 → 向量化 → 社区检测 → 描述生成
   - 每个阶段都可以独立运行和恢复
   - 缓存机制确保断点续传

2. **速率限制应对策略**：
   - 预先了解 API 限制
   - 设计合理的重试和退避策略
   - 利用缓存减少重复调用

3. **知识图谱应用价值**：
   - 自动从文献中提取结构化知识
   - 实体和关系可用于智能问答
   - 为后续的 RAG 系统提供知识基础

### 5.6 数据覆盖率分析

**数据现状**：

知识图谱当前仅基于少量数据构建，尚未覆盖完整数据集：

| 类别           | 已处理      | 待处理      | 说明                                                              |
| -------------- | ----------- | ----------- | ----------------------------------------------------------------- |
| **已处理**     | 16个txt文件 | -           | 生成695实体 + 523关系                                             |
| **中英文文献** | -           | 1,321篇PDF  | 力致变色纤维(225)、生物基纤维(14)、陶瓷纤维(1038)、高性能纤维(44) |
| **教材课本**   | -           | 104本PDF    | 第1-5批教材、纤维知识大模型教材(46本)                             |
| **总计**       | 16个文件    | 1,263个文件 | 数据覆盖率仅 1.27%                                                |

**原因分析**：

1. **测试阶段**：先用少量数据验证 GraphRAG 流程可行性
2. **PDF未转换**：大部分数据是PDF格式，需要先提取文本转换为txt
3. **API成本**：完整处理需要大量API调用，时间和成本较高

**后续规划**：

建议采用分批处理策略：
- **优先级1**：纤维知识大模型教材（46本）- 核心知识，质量高
- **优先级2**：高性能纤维文献（44篇）- 专业领域知识
- **优先级3**：其他文献和教材 - 补充完善

预计完整知识图谱将包含：
- 实体数量：10,000+ 个
- 关系数量：7,000+ 个
- 覆盖材料学科的完整知识体系

### 5.7 后续计划

- [ ] 等待 API 速率限制重置后继续运行
- [ ] 完成描述生成阶段（剩余 85.7%）
- [ ] 数据迁移到 Milvus 向量数据库
- [ ] 开发图谱查询 API
- [ ] 开发基于知识图谱的问答系统

---

## 6. 知识图谱数据迁移

### 6.1 功能描述

将 GraphRAG 生成的知识图谱数据（parquet 格式）导入 PostgreSQL 数据库，为后续的查询服务提供数据基础。

### 6.2 技术实现

#### 数据迁移流程

```
GraphRAG 输出
├── entities.parquet (695个实体)
└── relationships.parquet (523个关系)
         ↓
   迁移脚本处理
         ↓
PostgreSQL 数据库
├── entities 表
│   ├── id (主键)
│   ├── name (实体名称)
│   ├── type (实体类型)
│   ├── description (描述)
│   └── created_at/updated_at
└── relationships 表
    ├── id (主键)
    ├── source_id (源实体ID)
    ├── target_id (目标实体ID)
    ├── relation_type (关系类型)
    ├── description (描述)
    └── weight (权重)
```

#### 核心技术栈

- **SQLAlchemy**: ORM 框架，实现数据库模型定义和操作
- **Pandas + PyArrow**: 读取 parquet 文件
- **PostgreSQL**: 存储知识图谱数据
- **tqdm**: 显示迁移进度

#### 数据模型设计

**实体表（entities）**：
- `id`: 自增主键
- `name`: 实体名称（最长500字符）
- `type`: 实体类型（MATERIAL、PROPERTY、PROCESS等）
- `description`: 实体描述（文本类型）
- `source_doc`: 来源文档
- `created_at/updated_at`: 时间戳

**关系表（relationships）**：
- `id`: 自增主键
- `source_id`: 源实体ID（外键）
- `target_id`: 目标实体ID（外键）
- `relation_type`: 关系类型
- `description`: 关系描述
- `weight`: 关系权重
- `source_doc`: 来源文档

#### 关键代码实现

```python
# 实体名称到ID的映射
entity_name_to_id = {entity.name: entity.id for entity in entities}

# 批量插入关系
for idx, row in tqdm(relationships_df.iterrows()):
    source_id = entity_name_to_id.get(row.get('source'))
    target_id = entity_name_to_id.get(row.get('target'))
    
    if source_id and target_id:
        relationship = Relationship(
            source_id=source_id,
            target_id=target_id,
            relation_type=row.get('type', 'RELATED_TO'),
            description=str(row.get('description', '')),
            weight=float(row.get('weight', 1.0))
        )
        db.add(relationship)
```

### 6.3 遇到的问题

#### 问题1：Docker 服务未启动

**现象**：运行迁移脚本时报错 `connection to server at localhost:5432 failed: Connection refused`。

**原因分析**：PostgreSQL 运行在 Docker 容器中，但 Docker Desktop 未启动。

**解决方案**：
1. 启动 Docker Desktop 应用
2. 等待 30 秒确保所有服务完全启动
3. 重新运行迁移脚本

**经验总结**：使用容器化服务时，要确保 Docker 服务正常运行。

#### 问题2：关系数据字段理解错误

**现象**：首次迁移后关系表为空（0条记录）。

**原因分析**：
- GraphRAG 的 `relationships.parquet` 文件中，`source` 和 `target` 字段存储的是**实体名称**，不是实体ID
- 初期尝试使用 ID 映射，导致所有关系都无法匹配

**解决方案**：
1. 检查 parquet 文件的实际内容，发现字段含义
2. 修改迁移逻辑：先加载所有实体，构建"实体名称 → ID"的映射字典
3. 使用实体名称查找对应的ID，然后插入关系

**验证结果**：成功导入 523 个关系。

**经验总结**：迁移数据前，务必先检查源数据的实际结构和字段含义，不要凭经验猜测。

### 6.4 迁移结果验证

**数据统计**：
- 实体总数：695个
- 关系总数：523个
- 迁移成功率：100%

**实体类型分布**：

| 实体类型    | 数量 | 占比  | 说明                         |
| ----------- | ---- | ----- | ---------------------------- |
| MATERIAL    | 266  | 38.3% | 聚酰亚胺、环氧树脂等材料     |
| PROPERTY    | 219  | 31.5% | 热稳定性、介电常数等性质     |
| PROCESS     | 112  | 16.1% | 溶胶-凝胶法、热压成型等工艺  |
| APPLICATION | 38   | 5.5%  | 航空航天、电子封装等应用     |
| TECHNOLOGY  | 22   | 3.2%  | 纳米复合技术、表面改性等技术 |
| STRUCTURE   | 14   | 2.0%  | 分子链结构、晶体结构等       |
| EQUIPMENT   | 10   | 1.4%  | 扫描电镜、热重分析仪等设备   |
| 其他        | 14   | 2.0%  | 温度、研究、地理等           |

**数据质量评估**：
- ✅ 所有实体都有名称和类型
- ✅ 实体描述完整且有意义
- ✅ 关系描述清晰，权重合理
- ✅ 数据已持久化到 PostgreSQL

### 6.5 学习收获

1. **数据迁移流程规范**：
   - 先检查源数据结构
   - 设计合理的目标数据模型
   - 编写迁移脚本并验证
   - 确保数据完整性和一致性

2. **SQLAlchemy ORM 使用**：
   - 定义模型类继承 Base
   - 使用 relationship 定义表间关系
   - 批量插入时使用 flush() 获取自增ID

3. **PostgreSQL 最佳实践**：
   - 为常用查询字段添加索引
   - 使用外键约束保证数据一致性
   - 设计合理的字段长度和类型

### 6.6 后续计划

- [ ] 开发实体查询 API
- [ ] 开发关系查询 API
- [ ] 实现图谱可视化展示
- [ ] 开发基于知识图谱的问答系统

---

## 待开发功能

> 以下功能将在后续开发过程中持续更新本文档

### 7. 完整环境启动与启动文档编写

### 7.1 功能描述

完成项目所有基础服务（Docker 容器、后端 FastAPI、前端 Vite）的完整启动验证，识别并修复启动过程中暴露的依赖问题，最终编写一份可复用的《环境启动.md》文档供后续开发参考。

这是项目从"能写代码"到"能跑起来"的关键一步。

### 7.2 技术实现

#### 启动流程设计

环境启动分四步进行，每一步都设计了验证手段：

1. **Docker Desktop 检查与启动**：通过 `Get-Process` 检查是否已在运行，避免重复启动
2. **Docker Compose 一键启动**：`docker compose up -d` 拉起 PostgreSQL、Redis、Milvus、MinIO、etcd 五个容器
3. **后端 uvicorn 启动**：使用 `--reload` 模式启用热重载，`--app-dir` 参数解决路径含空格的目录切换问题
4. **前端 Vite 启动**：`npm run dev` 启动开发服务器，监听 5173 端口

#### 启动验证机制

每个服务启动后都设计了**多维度验证**：

| 维度   | 验证手段                          | 判定标准                    |
| ------ | --------------------------------- | --------------------------- |
| Docker | `docker compose ps`               | 5 个容器全部 `(healthy)`    |
| 后端   | `Invoke-RestMethod /health`       | 返回 `{"status":"healthy"}` |
| 后端   | `Invoke-RestMethod /`             | 返回 FastAPI 默认响应       |
| 后端   | `Invoke-RestMethod /openapi.json` | 包含完整 API 端点描述       |
| 前端   | `Invoke-WebRequest /`             | HTTP 200 + 页面标题正确     |
| 前端   | `Test-NetConnection`              | 5173 端口 TCP 可达          |

#### 启动文档结构设计

《环境启动.md》按"问题-解决"思路组织，包含：

- **当前运行状态速查表**：一眼看清哪些服务在线
- **完整启动流程**：四步法 + 每步的成功标志
- **常见问题与解决方案**：沉淀 6 个高频坑（见 7.3 节）
- **一键启动脚本**：PowerShell 脚本 start_all.ps1，三个窗口同时拉起
- **快速验证清单**：启动后跑一遍就能确认所有服务正常

### 7.3 遇到的问题

#### 问题1：requirements.txt 中 openai 版本冲突

**现象**：
```
ERROR: Cannot install -r requirements.txt (line 20) and openai==1.57.0
because these package versions have conflicting dependencies.
The conflict is caused by:
  The user requested openai==1.57.0
  langchain-openai 0.3.0 depends on openai<2.0.0 and >=1.58.1
```

**原因分析**：`langchain-openai==0.3.0` 升级后要求 `openai>=1.58.1`，但 requirements.txt 锁定了 `openai==1.57.0` 旧版本。

**解决方案**：
1. 修改 `backend/requirements.txt`，将 `openai==1.57.0` 改为 `openai>=1.58.1,<2.0.0`
2. 重新执行 `pip install -r requirements.txt`
3. 验证：所有依赖正常导入

**经验总结**：Python 生态的依赖关系复杂，跨多个包的版本约束要相互兼容。锁定版本时要注意**传递依赖**的版本范围。

#### 问题2：pydantic-core 版本不兼容导致 FastAPI 启动崩溃

**现象**：
```
SystemError: The installed pydantic-core version (2.27.2) is incompatible with
the current pydantic version, which requires 2.46.4.
```

**原因分析**：
- 项目需要 pydantic 2.10.4（配套 fastapi 0.115.0）
- 实际安装时，pip 错误升级 pydantic-core 到 2.47.0
- 2.47.0 与 pydantic 2.10.4 不兼容（pydantic 2.10.4 内置检查要求 pydantic-core 2.27.2）

**解决方案**：
1. 强制重装 pydantic-core 2.27.2：`pip install --force-reinstall "pydantic-core==2.27.2"`
2. 验证 fastapi 可以正常 `import`

**经验总结**：当出现版本不兼容时，**回退到上一兼容版本**往往比"追新"更稳妥。pydantic/pydantic-core 这种强耦合的包，强行升级风险大。

#### 问题3：pip install 报 WinError 32 / WinError 5（文件被占用）

**现象**：
```
ERROR: Could not install packages due to an OSError: [WinError 32]
另一个程序正在使用此文件，进程无法访问。
'...\\venv\\lib\\site-packages\\torch\\_export\\db\\examples\\tensor_setattr.py'
```

**原因分析**：
- Windows 下 pip install 大依赖（如 torch、transformers）时，后台可能有其他进程（Windows Defender 实时防护、文件索引服务、甚至是上一次未结束的 pip 进程）持有文件句柄
- pip 试图覆盖正在被读取的文件，导致 IO 错误

**解决方案**：
1. **杀掉所有相关进程**：`Get-Process -Name "python", "pip" | Stop-Process -Force`
2. **等待 30 秒**让 Windows 释放文件句柄
3. **分步安装**：先装小依赖（fastapi、sqlalchemy、psycopg2）验证能装上，再装大依赖（torch、transformers）
4. 如还失败，**重新创建 venv** 是最干净的解决方式

**经验总结**：Windows 平台是开发体验的"重灾区"，文件锁机制比 Linux/Mac 严格得多。处理大依赖时，要有耐心和备用方案。

#### 问题4：PowerShell 路径含空格导致命令解析失败

**现象**：
```
cd : 无法将"cd"项识别为 cmdlet
.\venv\Scripts\python.exe : 无法识别
```

**原因分析**：PowerShell 把 `cd D:\桌面\agent\Material\backend` 中的空格当成了命令分隔符，等价于执行了两条命令 `cd D:\桌面\agent\Material\backend` 和 `.\venv\Scripts\python.exe ...`——而第二条命令在原目录下找不到 python.exe。

**解决方案**：
- **方案 A**：用 `pushd "D:\桌面\agent\Material\backend"` 替代 `cd`，pushd 自带路径引用处理
- **方案 B**：路径外加引号：`cd "D:\桌面\agent\Material\backend"`
- **方案 C（推荐）**：使用 uvicorn 的 `--app-dir` 参数，根本不用切目录：
  ```powershell
  python -m uvicorn app.main:app --app-dir "D:\...\backend"
  ```

**经验总结**：含空格的路径在 Windows 命令行是"经典坑"。养成"路径必加引号"的习惯能避免大多数此类问题。

#### 问题5：Terminal 工具自动给命令加 `^U` 控制字符

**现象**：通过 run_in_terminal 提交 `cd` / `Set-Location` 命令时，命令前自动加了 `^U`（清除行）控制字符，导致命令变成 `^Ucd ...` 无法识别。

**原因分析**：Terminal 工具的输入处理与 PSReadLine 交互有冲突。

**解决方案**：
- 用 `pushd` 代替 `cd` 和 `Set-Location`——`pushd` 不触发 PSReadLine 的特殊处理
- 或用 `& "完整\路径\python.exe" -m ...` 形式直接执行，绕开 cd

#### 问题6：PowerShell 控制台中文编码乱码

**现象**：`Invoke-RestMethod` 返回的中文 API 标题显示为 `æå¦é¢æºè½å¦ä¹`¹³°`

**原因分析**：Windows PowerShell 5.1 默认使用 GBK 编码（代码页 936），而 FastAPI 返回 UTF-8 编码的 JSON。

**解决方案**：
1. **不影响功能判断**——返回内容结构正确，只是显示乱码
2. **生产解决方案**：用 `chcp 65001` 切换到 UTF-8 代码页
3. **更优方案**：用 Invoke-WebRequest 替代 Invoke-RestMethod，配合 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`

**经验总结**：Windows 终端的中文编码问题是历史遗留，跨平台项目要预留编码适配方案。

### 7.4 学习收获

#### 1. 依赖管理的"优先级"思维

启动过程踩了三个依赖坑（openai、pydantic-core、文件占用），核心教训是：
- **生产环境的依赖要锁版本**，但**版本号要选相互兼容的**
- **遇到冲突先看传递依赖**，不要只盯着直接依赖
- **大依赖（torch）慎装**，装不上要分步、隔离、重装 venv

#### 2. Windows 开发的"工程化"对策

这次启动暴露了 Windows 平台的多个原生问题：
- 路径含空格的兼容（pushd / 引号 / --app-dir）
- 文件被占用（杀进程 / 分步装 / 重装 venv）
- 中文编码乱码（chcp 65001 / .NET Encoding）

针对这些，文档化的"踩坑记录"比临时搜索更有效率。

#### 3. 启动验证的"多维度"原则

每个服务只用一种方式验证是不够的。健康检查、HTTP 状态码、API 内容断言、端口可达性、容器状态——多维度交叉验证才能避免"看似启动成功实际服务异常"的陷阱。

#### 4. 文档化是"复利资产"

《环境启动.md》编写时间 30 分钟，但**未来节省的时间是指数级**的：
- 新成员 onboarding
- 自己过几个月后回归
- 解决新出现的同类问题

把"启动过程"沉淀为"启动文档"，是这个项目走向工业化的第一步。

### 7.5 启动结果

**最终启动状态（2026-06-02 13:45）**：

| 服务              | 地址                       | 状态       |
| ----------------- | -------------------------- | ---------- |
| 前端 (Vite 6.4.2) | http://localhost:5173      | ✅ HTTP 200 |
| 后端 (FastAPI)    | http://localhost:8000      | ✅ healthy  |
| Swagger 文档      | http://localhost:8000/docs | ✅ 可访问   |
| PostgreSQL 16     | localhost:5432             | ✅ healthy  |
| Redis 7           | localhost:6379             | ✅ healthy  |
| Milvus 2.5.0      | localhost:19530            | ✅ healthy  |
| MinIO             | localhost:9000 / 9001      | ✅ healthy  |
| etcd              | localhost:2379             | ✅ healthy  |

**已交付文档**：
- 📄 `环境启动.md`（450+ 行）：完整的启动指南 + 6 个常见问题解决方案 + 一键启动脚本

**对项目的价值**：
1. 后续任何人（团队成员、未来自己）都能在 5 分钟内把项目跑起来
2. 启动过程中遇到的 6 个"Windows 平台 + Python 依赖"经典坑都已沉淀
3. 验证了从 Docker 到前后端全链路的连通性，证明项目架构可运行

### 7.6 后续计划

- [ ] 开发基于知识图谱的查询 API（Entity/Relationship/Search）
- [ ] 开发简单的前端知识图谱浏览界面
- [ ] 完善其他服务（Celery 异步任务、MinIO 文件上传等）的启动验证
- [ ] 引入 CI/CD 自动化启动验证

---

## 8. 一键启动/停止脚本开发

### 8.1 功能描述

在前一阶段完成"完整环境启动 + 文档"之后，下一步是将启动过程**脚本化**，让开发同学每天开机只需要运行一条命令即可拉起整个开发环境（包括 Docker、Python 依赖、后端 FastAPI、前端 Vite），关闭环境同理。

最终交付物：

- `start.ps1` —— 一键启动脚本（项目根目录）
- `stop.ps1` —— 一键停止脚本（项目根目录，与 `start.ps1` 配套）
- `环境启动.md` 同步更新，文档化脚本用法

### 8.2 技术实现

**核心设计理念**：

- **幂等性优先**：脚本可以重复运行，已在运行的服务不会被重复启动
- **健壮的等待策略**：每个外部依赖（Docker、容器健康、HTTP 端口）都有超时与轮询
- **Windows 编码兼容**：脚本使用 UTF-8 with BOM 编码，避免 PowerShell 5.1 默认按 GBK 解析报错
- **零侵入**：脚本仅做"启动编排"，不修改任何项目代码或配置

**5 步启动流程**（`start.ps1`）：

1. **检查 Docker Desktop** —— 调用 `docker info`，未就绪时每 3 秒重试一次，最多等 60 秒
2. **启动 Docker Compose** —— 执行 `docker compose up -d`，然后轮询 `docker compose ps --format json` 等所有服务 `State=running` 且 `Health=healthy`，最多等 90 秒
3. **检查 Python venv** —— 验证 `backend/venv/Scripts/python.exe` 存在；缺失则 `python -m venv` 创建；调用 `pip show fastapi` 验证依赖，缺失则自动 `pip install -r requirements.txt`
4. **启动后端** —— 先用 `Get-NetTCPConnection` 检查 8000 端口是否已被占用（被占用视为"已在运行"），否则用 `Start-Process cmd.exe /c ... > backend.log` 在后台启动 uvicorn，然后用 `Invoke-WebRequest /health` 轮询健康检查
5. **启动前端** —— 同样先检查 5173 端口，再用 `Start-Process` 后台跑 `npm run dev`，轮询 HTTP 200 验证

**停止流程**（`stop.ps1`）：

- 用 `Get-NetTCPConnection -LocalPort 8000/5173` 找出占用进程并 `Stop-Process -Force`
- 用 `Read-Host` 询问是否停止 Docker，区分"只停前后端"和"全停"
- 全停用 `docker compose stop`（保留数据卷），如需清空需手动 `docker compose down -v`

**关键工具命令**：

- `Start-Process -FilePath cmd.exe -ArgumentList "/c", $cmd -WindowStyle Hidden` —— 在隐藏窗口跑后台命令，把 stdout/stderr 重定向到 `.log` 文件，避免阻塞脚本
- `Get-NetTCPConnection -LocalPort X -State Listen` —— 取代 Linux 的 `lsof -i:X`，查找 Windows 上占用某端口的进程
- `Invoke-WebRequest -UseBasicParsing` —— 最小化依赖的 HTTP 探测

**幂等设计细节**：

- 启动前先查端口 → 已占用则跳过
- 启动后用 `Read-Host` 让用户决定是否自动开浏览器，避免硬编码行为

### 8.3 遇到的问题

**问题 1：PowerShell 5.1 中文编码乱码**

最初通过 `create_file` 工具写出的 ps1 脚本，在 PowerShell 5.1 终端直接报语法错误：中文显示成"绛夊緟涓?.."，`?` 是被截断的字节，"秒"等字符丢失。

**根因分析**：

- 工具默认输出 UTF-8 without BOM
- PowerShell 5.1 默认按系统 ANSI（中文系统是 GBK）解析文件
- UTF-8 字节被错误地按 GBK 解码，中文变成乱码，BMP 外的特殊符号（`▶`、`⚠`）变成 `?`

**解决方案**：

- 用 PowerShell 的 `New-Object System.Text.UTF8Encoding $true` 构造带 BOM 的编码器
- 通过 `WriteAllText` 重写文件，强制写入 BOM 头（`EF BB BF`）
- PowerShell 5.1 看到 BOM 后自动切换为 UTF-8 解析
- 为兼容性，最初版用了大量中文提示；最终重写时改为**全部使用英文提示**，从根本上回避编码陷阱
- 脚本运行时仍保留 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` 以保证终端输出不乱码

**问题 2：脚本执行策略被拦截**

用户默认 PowerShell 执行策略为 `Restricted`，双击 ps1 不会运行。

**解决方案**：

- 文档中明确给出绕过命令：`powershell -ExecutionPolicy Bypass -File .\start.ps1`
- 双击方式推荐"右键 → 使用 PowerShell 运行"
- 把绕过命令写进 `环境启动.md` 的脚本说明里

**问题 3：后台启动后如何判断"已就绪"**

最初版本用 `Start-Sleep` 固定等待 10 秒，简单但不可靠：冷启动可能 30 秒还不够，热启动又会空等。

**解决方案**：

- 后端用 `Invoke-WebRequest http://localhost:8000/health` 轮询，HTTP 200 才算就绪
- 前端用 `Invoke-WebRequest http://localhost:5173` 轮询 HTTP 200
- Docker 用 `docker compose ps --format json` 解析每个容器的 `Health` 字段
- 三处都加 60~90 秒超时和实时"等待中"提示，避免看起来"卡住"

**问题 4：路径含空格**

项目路径 `D:\桌面\agent\Material\` 包含空格和中文。`Start-Process` 拼接命令行时如果不加引号会断词。

**解决方案**：

- 所有路径都用双引号包裹：`"cd /d \"$PROJECT_ROOT\backend\""`
- 使用 `$PROJECT_ROOT = $PSScriptRoot` 动态获取脚本所在目录，避免硬编码
- 在 `环境启动.md` 显式提醒用户"路径含空格/中文也能跑"

### 8.4 学习收获

- **PowerShell 5.1 的编码陷阱**：UTF-8 BOM 是兼容老版本 Windows 的关键，`create_file` 工具生成的 UTF-8 no-BOM 文件经常踩坑
- **`Start-Process -WindowStyle Hidden` + 日志重定向**：是 PowerShell 做"守护进程式"启动的惯用模式，比 `Start-Process powershell -NoExit` 更干净
- **幂等性是脚本的"基本礼貌"**：一个好的启动脚本必须能重复运行
- **健康检查优于固定等待**：用 HTTP 探测代替 `Start-Sleep`，响应更准、用户体验更好
- **PowerShell 可以用纯 ASCII 写出可维护脚本**：用 `Write-Host` 输出英文提示 + 颜色编码，文件就完全避开编码问题

### 8.5 验证结果

- ✅ 实测 `start.ps1`：5 步全部打印 `[OK]`，最终显示"All services started successfully!"
- ✅ 实测 `start.ps1` 幂等性：再次运行时检测到 8000/5173 已被占用，自动跳过启动
- ✅ `backend.log` 和 `frontend.log` 正确生成，后端 `/health` 返回 200
- ✅ `环境启动.md` 同步更新，新增"一键启动/停止脚本"章节，把原来的 `start_all.ps1` 模板替换为新脚本说明
- ✅ `任务清单.md` 新增"一键脚本"条目

### 8.6 后续计划

- [ ] 把 `start.ps1` / `stop.ps1` 打包成 `.bat` 调用入口，进一步降低双击门槛
- [ ] 增加"自动健康巡检"模式：脚本启动后每 30 秒检查一次服务状态，异常时打印提示
- [ ] 集成到 VS Code Tasks：让用户能 `Ctrl+Shift+B` 直接跑启动
- [ ] 编写一份 `scripts/README.md` 集中说明项目所有运维脚本

---

## 待开发功能

> 以下功能将在后续开发过程中持续更新本文档

### 9. 智能问答服务开发

**状态**：待开发

**预计内容**：
- RAG 架构实现
- 向量检索与关键词检索混合策略
- Prompt 工程

### 10. 图片检索与关联

**状态**：待开发

**预计内容**：
- 图片向量化
- 图片-知识点关联构建
- 多模态检索

### 11. PPT自动生成

**状态**：待开发

**预计内容**：
- python-pptx 库使用
- 模板设计
- 内容规划算法

---

## 项目总结与反思

### 技术亮点

1. **现代化技术栈**：采用 FastAPI + React 的全栈架构，技术栈先进
2. **容器化部署**：使用 Docker Compose 统一管理所有服务，部署简单
3. **异步编程**：充分利用 Python 异步特性，提升并发性能
4. **知识图谱**：引入 GraphRAG 实现自动化知识抽取，技术前沿

### 遇到的挑战

1. **LLM API 适配**：不同厂商的 API 格式差异需要额外处理，兼容性问题突出
2. **中英文混合内容**：需要针对不同语言优化处理策略，增加复杂度
3. **向量维度变化**：从 1024 维迁移到 2048 维，需要调整配置和存储结构

### 个人成长

1. **全栈开发能力**：从后端到前端，从数据库到 AI 服务，综合能力提升
2. **问题解决能力**：学会了系统性排查问题的方法，从现象到原因到解决方案
3. **文档编写能力**：学会了编写清晰的技术文档，提升沟通表达能力

---

## 附录

### 参考文档

- [FastAPI 官方文档](https://fastapi.tiangolo.com/)
- [智谱 AI API 文档](https://open.bigmodel.cn/dev/api)
- [GraphRAG 文档](https://microsoft.github.io/graphrag/)
- [PyMuPDF 文档](https://pymupdf.readthedocs.io/)

### 常用命令

- 启动所有服务：`docker compose up -d`
- 启动后端开发服务器：`cd backend && python -m uvicorn app.main:app --reload`
- 启动前端开发服务器：`cd frontend && npm run dev`
- 运行 GraphRAG：`python -m graphrag index --root ./graphrag --verbose`

---

**文档版本**：v1.0  
**最后更新**：2026-05-21
