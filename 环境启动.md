# 材料学院智能学习平台 - 环境启动指南

> 本文档基于 **2026-06-02 首次完整启动** 的真实经验编写，记录了启动过程中遇到的所有坑和最终验证可行的命令。

---

## ✅ 当前运行状态

| 服务               | 地址                                | 状态                     |
| ------------------ | ----------------------------------- | ------------------------ |
| 前端 (Vite)        | http://localhost:5173               | ✅ 运行中（HTTP 200）     |
| 后端 (FastAPI)     | http://localhost:8000               | ✅ 运行中                 |
| API 文档 (Swagger) | http://localhost:8000/docs          | ✅ 可访问                 |
| 健康检查           | http://localhost:8000/health        | ✅ `{"status":"healthy"}` |
| PostgreSQL         | localhost:5432                      | ✅ Docker healthy         |
| Redis              | localhost:6379                      | ✅ Docker healthy         |
| Milvus             | localhost:19530                     | ✅ Docker healthy         |
| MinIO              | http://localhost:9000 / 控制台 9001 | ✅ Docker healthy         |
| etcd               | localhost:2379                      | ✅ Docker healthy         |

---

## 📋 启动前准备

### 必备软件

| 软件           | 版本   | 说明                                   |
| -------------- | ------ | -------------------------------------- |
| Docker Desktop | 最新版 | 用于启动 PostgreSQL/Redis/Milvus/MinIO |
| Python         | 3.10+  | 后端运行环境                           |
| Node.js        | 18+    | 前端运行环境                           |
| Git            | -      | 可选                                   |

### 项目根目录结构

```
Material/
├── backend/          # 后端服务
├── frontend/         # 前端服务
├── docker-compose.yml
├── .env              # 环境变量（已配置）
└── 环境启动.md       # 本文档
```

---

## 🚀 完整启动流程

### 步骤 1：启动 Docker Desktop

```powershell
# 检查 Docker Desktop 是否已运行
Get-Process -Name "Docker Desktop" -ErrorAction SilentlyContinue

# 如果没运行，启动它
Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"

# 等待 30-60 秒，直到引擎就绪
Start-Sleep -Seconds 30
```

**验证**：`docker ps` 不报错即为就绪。

---

### 步骤 2：启动所有 Docker 服务

在项目根目录执行：

```powershell
cd D:\桌面\agent\Material
docker compose up -d
```

**预期输出**：5 个容器全部 `Running`

```
✔ Container material_minio    Running
✔ Container material_postgres Running
✔ Container material_redis    Running
✔ Container material_etcd     Running
✔ Container material_milvus   Running
```

等待 15-30 秒后查看健康状态：

```powershell
docker compose ps
```

每个容器都应该是 `(healthy)` 状态。

---

### 步骤 3：启动后端服务（新开终端窗口）

> ⚠️ **关键**：PowerShell 中切到含空格目录，必须用 `pushd` 或在路径外加引号。

```powershell
# 启动后端（前台运行）
pushd "D:\桌面\agent\Material\backend"
.\venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

或者用 `--app-dir` 参数（无需 cd）：

```powershell
& "D:\桌面\agent\Material\backend\venv\Scripts\python.exe" -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --app-dir "D:\桌面\agent\Material\backend"
```

**成功标志**：

```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**验证**：

```powershell
# 健康检查
Invoke-RestMethod -Uri "http://localhost:8000/health"
# 应返回: {"status": "healthy"}
```

---

### 步骤 4：启动前端服务（再开一个终端窗口）

```powershell
pushd "D:\桌面\agent\Material\frontend"
npm run dev -- --host 0.0.0.0 --port 5173
```

**成功标志**：

```
VITE v6.4.2  ready in 1362 ms
➜  Local:   http://localhost:5173/
```

**验证**：

```powershell
# 浏览器访问
start http://localhost:5173

# 或命令行检查
Invoke-WebRequest -Uri "http://localhost:5173/" -Method GET
# StatusCode: 200
```

---

## 🔧 服务端口速查

| 服务         | 端口      | 用途               | 凭据                        |
| ------------ | --------- | ------------------ | --------------------------- |
| 前端         | 5173      | Vite 开发服务器    | -                           |
| 后端 API     | 8000      | FastAPI 服务       | -                           |
| API 文档     | 8000/docs | Swagger UI         | -                           |
| PostgreSQL   | 5432      | 数据库             | `postgres` / `postgres`     |
| Redis        | 6379      | 缓存 / Celery 队列 | -                           |
| Milvus       | 19530     | 向量数据库         | -                           |
| MinIO API    | 9000      | 对象存储           | `minioadmin` / `minioadmin` |
| MinIO 控制台 | 9001      | Web 管理界面       | `minioadmin` / `minioadmin` |
| etcd         | 2379      | Milvus 依赖        | -                           |

---

## 🛑 关闭服务

按启动顺序**反向关闭**：

```powershell
# 1. 前端 / 后端：直接 Ctrl+C 关闭终端

# 2. 停止所有 Docker 容器（保留数据）
docker compose stop

# 如需彻底清理（包括数据卷，慎用！）
docker compose down -v
```

---

## ⚠️ 常见问题与解决方案

### Q1：pip install 报 `openai` 与 `langchain-openai` 版本冲突

**现象**：
```
ERROR: Cannot install -r requirements.txt (line 20) and openai==1.57.0
because these package versions have conflicting dependencies.
```

**原因**：`langchain-openai==0.3.0` 要求 `openai>=1.58.1`，但 `requirements.txt` 锁定了 `openai==1.57.0`。

**解决**：修改 `backend/requirements.txt`：

```diff
- openai==1.57.0
+ openai>=1.58.1,<2.0.0
```

---

### Q2：FastAPI 启动报 `pydantic-core` 版本不兼容

**现象**：
```
SystemError: The installed pydantic-core version (2.27.2) is incompatible with
the current pydantic version, which requires 2.46.4.
```

**原因**：`pip install -r requirements.txt` 在 Windows 上可能与后台进程冲突（`WinError 32 文件被占用`），导致 `pydantic-core` 被错误升级到 2.47.x。

**解决**：

```powershell
# 强制降级回兼容版本
pushd "D:\桌面\agent\Material\backend"
.\venv\Scripts\python.exe -m pip install --force-reinstall "pydantic-core==2.27.2"
```

---

### Q3：pip 报 `WinError 32` / `WinError 5` 文件被占用

**现象**：
```
ERROR: Could not install packages due to an OSError: [WinError 32]
另一个程序正在使用此文件，进程无法访问。
```

**原因**：之前的 `pip install` 后台进程还在使用 venv 中的文件，或 Windows Defender / 索引服务正在扫描。

**解决**：

1. 杀掉所有相关进程：
   ```powershell
   Get-Process -Name "python", "pip" -ErrorAction SilentlyContinue | Stop-Process -Force
   ```

2. 等待 30 秒，再重新运行 `pip install`。

3. 如果还不行，**先单独装核心包**再装其余：
   ```powershell
   # 先装关键包
   .\venv\Scripts\python.exe -m pip install fastapi uvicorn sqlalchemy psycopg2-binary pydantic pydantic-core==2.27.2
   # 再装其余
   .\venv\Scripts\python.exe -m pip install -r requirements.txt
   ```

---

### Q4：PowerShell 路径含空格导致 `cd` 失败

**现象**：
```
cd : 无法将"cd"项识别为 cmdlet
.\venv\Scripts\python.exe : 无法识别
```

**原因**：PowerShell 把空格当成了命令分隔符。

**解决**：
- 用 `pushd "D:\桌面\agent\Material\backend"` 替代 `cd`
- 或在路径外加引号：`cd "D:\桌面\agent\Material\backend"`
- 或使用 `--app-dir` 参数启动（无需 cd）

---

### Q5：端口被占用

**现象**：
```
bind: address already in use
```

**解决**：
```powershell
# 查找占用进程
netstat -ano | findstr :8000

# 结束进程
taskkill /PID <进程ID> /F
```

---

### Q6：Docker 容器无法启动

**现象**：`docker compose up` 后容器一直 `Restarting` 或 `Exit 1`

**排查**：
```powershell
docker compose logs postgres  # 查看具体服务日志
```

常见原因：
- 5432/6379/9000 等端口被其他程序占用
- 之前的数据卷损坏（执行 `docker compose down -v` 清理）

---

## 📝 一键启动/停止脚本（推荐）

项目根目录已经提供了两个 PowerShell 脚本，**直接双击运行或右键 → 使用 PowerShell 运行**即可：

| 脚本        | 作用                                     | 何时用          |
| ----------- | ---------------------------------------- | --------------- |
| `start.ps1` | 一键启动全部服务（Docker + 后端 + 前端） | 每天开机/重启用 |
| `stop.ps1`  | 一键停止前后端进程（可选停止 Docker）    | 下班/不开发时   |

### 🚀 启动：`start.ps1`

脚本会自动完成以下 5 步，并实时显示状态：

1. **检查 Docker Desktop** —— 等待引擎就绪（最多 60s）
2. **启动 Docker Compose 服务** —— PostgreSQL/Redis/Milvus/MinIO/etcd，等待全部 `healthy`
3. **检查 Python 虚拟环境** —— 若缺失则自动创建；若依赖未装则自动 `pip install`
4. **启动后端** —— FastAPI 监听 8000，轮询 `/health` 验证
5. **启动前端** —— Vite 监听 5173，HTTP 200 验证

**幂等设计**：脚本会先检查端口是否被占用，已运行的服务会自动跳过。

**日志**：
- 后端日志：`backend.log`
- 前端日志：`frontend.log`
- Docker 日志：`docker compose logs -f`

**命令行运行方式**（绕过执行策略）：

```powershell
# 在项目根目录执行
powershell -ExecutionPolicy Bypass -File .\start.ps1
```

### 🛑 停止：`stop.ps1`

- 自动结束占用 8000 / 5173 端口的进程（后端 + 前端）
- 询问是否同时停止所有 Docker 服务（输入 `n` 可保留容器运行）
- 完成后按 Enter 退出

```powershell
powershell -ExecutionPolicy Bypass -File .\stop.ps1
```

### ✅ 实测验证

脚本已在 **2026-06-02** 完成首次实测：

- ✅ Docker Desktop 检测正常
- ✅ 5 个容器全部 healthy
- ✅ Python venv 自动检查并通过
- ✅ 后端 8000 端口健康检查通过
- ✅ 前端 5173 端口 HTTP 200
- ✅ 幂等性验证：已运行服务不会被重复启动

> 💡 如果 `start.ps1` 报红字错误，**不要慌**：先看是 Docker 启动慢（脚本最长等 60s）、venv 缺失（自动重建）、还是 pip install 卡住（重新跑即可）。

---

## 🧪 启动后快速验证清单

```powershell
# 1. Docker 服务
docker compose ps
# 期望：5 个容器全 healthy

# 2. 后端 API
Invoke-RestMethod -Uri "http://localhost:8000/health"
# 期望：{"status": "healthy"}

# 3. 后端根端点
Invoke-RestMethod -Uri "http://localhost:8000/"
# 期望：返回 FastAPI 默认响应

# 4. API 文档
start http://localhost:8000/docs
# 期望：浏览器打开 Swagger UI

# 5. 前端
start http://localhost:5173
# 期望：浏览器打开"材料学院智能学习平台"页面

# 6. 数据库连接
$env:PGPASSWORD = "postgres"
& "C:\Program Files\PostgreSQL\16\bin\psql.exe" -h localhost -U postgres -d material_learning -c "SELECT count(*) FROM entities;"
# 期望：返回 695（已迁移的实体数）
```

---

## 📚 相关文档

- [README.md](README.md) - 项目总览
- [开发指南](docs/开发指南.md) - 详细开发规范
- [PRD](prd.md) - 产品需求
- [技术需求](技术需求.md) - 技术方案
- [任务清单](任务清单.md) - 当前进度

---

## 📅 启动记录

| 日期       | 启动结果 | 备注                                                               |
| ---------- | -------- | ------------------------------------------------------------------ |
| 2026-06-02 | ✅ 成功   | 首次完整启动，发现并修复 openai 版本冲突、pydantic-core 兼容性问题 |
| 2026-06-02 | ✅ 脚本   | 完成 `start.ps1` / `stop.ps1` 一键脚本开发并通过实测，幂等性正常   |
