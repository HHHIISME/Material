# 材料学院智能学习平台任务清单

## 📋 项目概览

| 项目     | 内容                       |
| -------- | -------------------------- |
| 项目名称 | 材料学院智能学习平台       |
| 开始日期 | 2026-05-19                 |
| 预计周期 | 16周                       |
| 当前阶段 | 阶段一：基础设施搭建完成 ✅ |

**最后更新**: 2026-06-02 17:30

---

## ✅ 当前完成进度

### 已完成项目

| 类别               | 完成内容                                                                         | 状态       |
| ------------------ | -------------------------------------------------------------------------------- | ---------- |
| **项目初始化**     | Git仓库、分支策略、开发环境                                                      | ✅ 完成     |
| **Docker服务**     | PostgreSQL, Redis, Milvus, MinIO, etcd                                           | ✅ 运行中   |
| **后端框架**       | FastAPI项目结构、基础API                                                         | ✅ 运行中   |
| **前端框架**       | React + Vite + TypeScript                                                        | ✅ 运行中   |
| **API Key**        | 智谱GLM-4 API Key已申请                                                          | ✅ 配置     |
| **智谱API测试**    | chat/embed/batch_embed 接口测试通过                                              | ✅ 完成     |
| **PDF解析服务**    | 文本提取、图注识别、页面渲染                                                     | ✅ 完成     |
| **图片提取**       | PDF图片提取功能（暂缓，后续实现）                                                | ⏸️ 暂缓     |
| **文档方案**       | 图片-知识关联方案、知识图谱方案已确定                                            | ✅ 完成     |
| **知识图谱构建**   | GraphRAG改造完成、实体关系抽取（数据覆盖率1.27%）                                | ⚠️ 部分完成 |
| **增量更新规划**   | 设计增量更新方案（脚本/API/任务跟踪），等待开发                                  | ✅ 已规划   |
| **管理员投放规划** | 文档上传 + 异步解析 + 增量构建前端方案已规划                                     | ✅ 已规划   |
| **数据迁移**       | 知识图谱数据导入PostgreSQL（695实体+523关系）                                    | ✅ 完成     |
| **环境启动**       | 完整启动验证（Docker+后端+前端），编写启动文档                                   | ✅ 完成     |
| **一键脚本**       | `start.ps1` / `stop.ps1` 一键启动停止脚本，幂等性通过                            | ✅ 完成     |
| **图谱查询 API**   | 11 个端点：实体 5 + 关系 4 + 图谱 3（path 含 BFS 寻路 + 自环/不连通/多跳健壮性） | ✅ 完成     |
| **图谱可视化**     | 1 个页面 3 模式（图谱总览 / 子图浏览 / 路径寻路）+ react-force-graph-2d 力导向图 | ✅ 完成     |

### 服务运行状态

| 服务       | 地址                                 | 状态     |
| ---------- | ------------------------------------ | -------- |
| PostgreSQL | localhost:5432                       | ✅ 运行中 |
| Redis      | localhost:6379                       | ✅ 运行中 |
| Milvus     | localhost:19530                      | ✅ 运行中 |
| MinIO      | localhost:9000 (API) / 9001 (控制台) | ✅ 运行中 |
| etcd       | localhost:2379                       | ✅ 运行中 |
| 后端API    | http://localhost:8000                | ✅ 运行中 |
| 前端应用   | http://localhost:5173                | ✅ 运行中 |

### 📝 任务规划说明

> **为什么只做 KnowledgeGraph（任务二）？**
> 
> GraphRAG 是一个完整的知识处理流程，会自动完成：
> - ✅ 文本分块
> - ✅ 实体/关系抽取（构建知识图谱）
> - ✅ 向量化（支持语义检索）
> 
> 因此，**无需单独开发向量化入库模块**，GraphRAG 已包含此功能。
> 
> 后续添加图片时，可增量添加图片节点和关系，不会重复计算已有数据。

### 下一步任务（2026-06-02 重排）

> **架构调整**：原 PDF 解析服务推翻重做；入库采用 **双路并行**（普通 RAG + GraphRAG），由 **LLM 意图路由** 决定走哪条；本期全部同步实现，异步拆分留待下期。

1. [x] 测试智谱API连通性 ✅
2. [x] PDF解析服务（旧版）✅ → [ ] **PDF解析服务 v2 重建**（见 2.1）
3. [ ] **普通 RAG 入库通道**（splitter + batch embed + Milvus）
4. [ ] **GraphRAG 入库通道**（沿用 KnowledgeGraph 改造，entities/relationships 入 PG + 向量入 Milvus）
5. [ ] **LLM 意图路由**（用户问题 → 查事实 / 找关系）
6. [ ] **双路检索融合**（按路由结果选 RAG 或 GraphRAG 通道，输出带来源标注）
7. [ ] 文档投放（管理员上传 → 触发两路入库）

---

## 🎯 阶段一：项目准备与基础设施搭建（第1-2天）

### 1.1 项目初始化

- [x] **创建项目仓库**
  - [x] 初始化 Git 仓库
  - [x] 配置 .gitignore
  - [x] 创建分支策略（main/develop/feature）
  - [ ] 配置 CI/CD 流水线

- [x] **开发环境搭建**
  - [x] 安装 Python 3.10+
  - [x] 安装 Node.js 18+
  - [x] 配置虚拟环境
  - [x] 安装 Docker & Docker Compose

- [x] **项目文档完善**
  - [x] 编写 PRD 文档
  - [x] 编写技术需求文档
  - [x] 编写开发指南
  - [ ] 编写部署文档

---

### 1.2 基础设施搭建

- [x] **数据库部署**
  - [x] 部署 PostgreSQL (localhost:5432)
  - [x] 部署 Milvus 向量数据库 (localhost:19530)
  - [x] 部署 Redis 缓存 (localhost:6379)
  - [ ] 部署 Elasticsearch（可选）

- [x] **存储服务部署**
  - [x] 部署 MinIO 对象存储 (localhost:9000/9001)
  - [x] 配置存储桶（图片、PPT、文档）

- [x] **LLM 服务配置**
  - [x] 申请 zhipu API Key
  - [x] 测试 API 连通性 ✅
  - [x] 配置 API 调用封装层 ✅（zhipu_service.py）

---

## 📚 阶段二：知识库构建（第3-5天）

### 2.1 文档处理流水线（v2 重构）

> **目标**：输出统一的 `ParsedDocument` 中间表示（pages + 文本块 + 图片 + 表格 + 元数据），下游 RAG / GraphRAG 双路共用。
> **运行方式**：本期同步实现，函数边界预留 async 改造空间。

#### 优先级 P0 - 必须完成

- [ ] **PDF解析服务 v2**（旧版推翻重做）
  - [ ] 设计 `ParsedDocument` 中间结构（pages[].text / images[] / tables[] / metadata）
  - [ ] **PDF 类型检测**（文本型 / 扫描型 / 混合型）
    - [ ] 写 `detect_pdf_type()`：用 `fitz` 抽几页判断文字密度
    - [ ] 文本型 → PyMuPDF 直抽；扫描型 → 走 OCR；混合型 → 逐页判断
  - [ ] **文本提取**（PyMuPDF）
    - [ ] 按页抽文字 + 记录页码 / bbox
    - [ ] 处理乱码（编码检测 / 字体回退）
    - [ ] 保留阅读顺序（不是单纯 `get_text()`，要按 block 排序）
  - [ ] **图片提取**
    - [ ] 记录图片原始字节 + 页码 + bbox
    - [ ] 识别图注（正则：`图X-X` / `Fig.X` / `Figure X`）
    - [ ] 识别文本中图片引用（"如图X-X所示"），建立 image ↔ caption ↔ mention 三元关联
  - [ ] **表格提取**（pdfplumber）
    - [ ] 抽表格为 CSV-like 结构
    - [ ] 记录表注
  - [ ] **扫描型 OCR**（方案选型待定）
    - [ ] 候选：智谱 GLM-4V / PaddleOCR / Tesseract
    - [ ] 选型标准：中文识别率 / 部署成本 / 限流
  - [ ] **图片 AI 描述**（无图注的图片调 GLM-4V 生成）
    - [ ] 降采样到合理尺寸
    - [ ] 调用 GLM-4V 返回中文描述
    - [ ] 限流 + 重试（3 次指数退避）
  - [ ] **统一 API**
    - [ ] `POST /api/v1/documents/parse/pdf`（替换旧版）→ 返回 `ParsedDocument`
    - [ ] `POST /api/v1/documents/parse/pdf/text`（只取文本，快路径）
  - [ ] **验收标准**
    - [ ] 5 个不同类型 PDF 全部跑通（中文教材 / 英文论文 / 扫描版 / 表格密集 / 图文混排）
    - [ ] 解析结果 JSON schema 校验通过
    - [ ] 单 PDF 50 页平均解析时间 < 30s

- [ ] **Word文档解析**（顺带做）
  - [ ] 集成 python-docx 库
  - [ ] 提取文档结构（标题 / 段落 / 列表 / 嵌入图片）
  - [ ] 输出与 PDF 同样的 `ParsedDocument` 中间结构

- [ ] **文档元数据 Schema**
  - [ ] 字段：doc_id / source_path / sha256 / title / authors / year / language / parser_version
  - [ ] 落 Postgres `documents` 表

- [ ] **文档结构化处理**（v2 解析输出的二次加工）
  - [ ] 章节识别（基于 PDF 目录/标题样式）
  - [ ] 知识点分割（在 chunk 之上再做一次语义切分）
  - [ ] 元数据落 PG `documents` 表（doc_id / source_path / sha256 / title / authors / year / language / parser_version）

#### 优先级 P1 - 重要

- [ ] **公式识别**
  - [ ] 集成 LaTeX-OCR
  - [ ] 数学公式转 LaTeX 格式
  - [ ] 存储公式图片和 LaTeX 源码

- [ ] **图表处理**
  - [ ] 图表类型识别
  - [ ] 图表信息提取
  - [ ] 相图特殊处理

---

### 2.2 知识入库（双路并行：RAG + GraphRAG）

> **架构**：上游 PDF 解析 v2 产出 `ParsedDocument`，下游分两路：
> - **RAG 通道**（轻量级，适合查事实 / 找原文段落）  
> - **GraphRAG 通道**（基于现有 KnowledgeGraph 改造，适合找关系 / 做总结）
> 
> 两路产出分别存：RAG → Milvus `chunks` collection；GraphRAG → PG `entities/relationships` + Milvus `entity_embeddings` collection。
> 路由决策在阶段三「问答服务」里做。

#### 优先级 P0 - 必须完成

- [ ] **RAG 通道：文本向量化入库**
  - [ ] **文本分块**（用 LangChain `RecursiveCharacterTextSplitter`，先固化参数：chunk_size=512, overlap=64）
  - [ ] **批量 embed**（沿用 `ZhipuService.batch_embed`，加 retry/限流：`time.sleep(0.3)` + 3 次指数退避）
  - [ ] **Milvus 建 collection**（`chunks`）
    - [ ] 字段：chunk_id / doc_id / page_num / text / embedding(2048) / source_path
    - [ ] 索引：`IVF_FLAT`，nlist=1024，度量 `COSINE`
  - [ ] **Postgres 写元数据**（`chunks` 表：id / doc_id / page_num / text / milvus_id 双向指针）
  - [ ] **入库脚本** `scripts/ingest_rag.py --pdf <path>`（同步可跑通）
  - [ ] **验收标准**
    - [ ] 一篇 10 页 PDF 跑通，全文检索 top-5 命中相关段落

- [ ] **RAG 通道：图片索引**
  - [ ] 图片存储到 MinIO（`images` 桶）
  - [ ] AI 描述 → embedding-3 向量化
  - [ ] Milvus `image_embeddings` collection（字段：image_id / doc_id / page_num / caption / ai_description / embedding(2048) / minio_path）
  - [ ] Postgres `images` 表（id / doc_id / page_num / bbox / caption / minio_path / milvus_id）

- [ ] **GraphRAG 通道：图谱构建**（沿用 KnowledgeGraph 改造 ✅）
  - [x] settings.yaml 已改智谱 API + embedding-3 2048 维
  - [x] 中文 prompts 复用
  - [x] 695 实体 + 523 关系已抽出（覆盖率 1.27%）
  - [ ] **续跑描述生成**（上次卡在 3247/22760）
    - [ ] 节流参数调优（request_delay 1.0→3.0, max_input_tokens 12000→8000, max_retries 5→10）
    - [ ] 或：升级智谱标准套餐（¥120/月 RPM 60→600）+ 并发 3
  - [ ] **GraphRAG 实体向量化入库**（与 RAG 区分开）
    - [ ] Milvus `entity_embeddings` collection（字段：entity_id / name / type / description / embedding(2048)）
    - [ ] 索引：`IVF_FLAT`，nlist=1024，度量 `COSINE`
    - [ ] PG `entities.description_embedding_id` 加列关联
  - [ ] **跑全量图谱**
    - [ ] 优先处理 46 本纤维知识大模型教材
    - [ ] 跑完后全量迁移到 PG + Milvus

- [ ] **图片-知识关联**（v2 简化）
  - [ ] **不单独建关联表**（避免膨胀）
  - [ ] 改用：图片节点本身是图谱的一种 node type
  - [ ] 关系：`实体 --[illustrated_by]→ 图片`、`图片 --[depicts]→ 实体`
  - [ ] 抽取时一并进 GraphRAG 流水线

- [ ] **入库幂等性**
  - [ ] 同一 PDF 跑两次入库，Milvus / PG 数量不增、向量不变
  - [ ] 按 `(doc_id, chunk_id)` / `(entity_name, type)` 去重

- [ ] **数据导入验收**
  - [ ] 5 篇不同领域论文 / 教材跑通 RAG 通道
  - [ ] top-5 检索结果与人工标注吻合率 > 80%

---

### 2.3 知识图谱构建

#### 优先级 P1 - 重要（复用 KnowledgeGraph 项目）

**实施方案：混合架构**

```
┌────────────────────────────────────────────────────────────────┐
│                      整体数据流                                 │
└────────────────────────────────────────────────────────────────┘

PDF 文档
    │
    ▼
┌─────────────────────┐
│ 【新建模块】        │
│ PDF解析器           │
│ - 提取文本          │
│ - 提取图片          │
│ - 提取图注          │
└─────────┬───────────┘
          │
    ┌─────┴─────┐
    ▼           ▼
 文本块       图片文件
    │           │
    │           ▼
    │    ┌─────────────────────┐
    │    │ 【新建模块】        │
    │    │ 图片处理            │
    │    │ - 存储到 MinIO      │
    │    │ - AI描述(GLM-4V)    │
    │    │ - 关联构建          │
    │    └─────────┬───────────┘
    │              │
    └──────┬───────┘
           │
           ▼
    ┌─────────────────────┐
    │ 【改造 KnowledgeGraph】│
    │ - 修改配置为智谱API   │
    │ - 向量维度改为2048    │
    │ - graphrag index      │
    │ - 实体/关系抽取       │
    └─────────┬───────────┘
              │
              ▼
    ┌─────────────────────┐
    │ 输出产物             │
    │ - graph.graphml     │
    │ - entities.parquet  │
    │ - 向量索引          │
    └─────────┬───────────┘
              │
              ▼
    ┌─────────────────────┐
    │ 【数据迁移】         │
    │ - LanceDB → Milvus  │
    │ - 图谱存 PostgreSQL │
    │ - 图片-实体关联     │
    └─────────────────────┘
```

- [x] **KnowledgeGraph 改造** ✅（历史快照见 `reconnaissance.md`）
  - [x] settings.yaml 改为智谱 GLM-4-flash + embedding-3 (2048 维)
  - [x] prompts/ 中文提示词复用
  - [x] 索引构建 + 实体抽取首跑成功
  - [x] 数据迁移到 PostgreSQL（entities + relationships 表）✅

- [ ] **知识图谱全量构建**（详见 `reconnaissance.md` 2026-06-02 快照）
  - [ ] 续跑描述生成（2.2 里有清单，GraphRAG 通道那一节负责执行）

- [ ] **知识图谱增量更新**
  - [ ] 新增 `migrate_to_postgres.py --mode=incremental` 脚本（按 `(name, type)` 去重）
  - [ ] 实体已存在 → 更新 `description` / `updated_at`；不存在 → INSERT
  - [ ] 关系按 `(source_id, target_id, relation_type)` 去重
  - [ ] 写迁移日志（新增 N / 更新 M / 跳过 K）
  - [ ] 后端封装 `POST /api/v1/kg/reindex` 接口
  - [ ] 增量更新幂等性测试（同一批文件跑两次，结果一致）

- [ ] **图片-图谱关联**（v2 简化版，详见 2.2）
  - [ ] 不单独建关联表，图片作为图谱的 node type
  - [ ] 抽取时一并进 GraphRAG 流水线

- [x] **图谱查询服务**（11 个端点全部完成，11/11 通过 curl 验证）
  - [x] 实体查询 API（5 端点：列表/搜索/类型/详情/度）
  - [x] 关系查询 API（4 端点：列表/按实体/类型/详情）
  - [~] 关联图片查询 API（暂缓，图片节点未入库，后续补）
  - [x] 图谱可视化展示（前端 react-force-graph-2d 力导向图）

- [ ] **管理员文档投放**（重做：触发双路入库）
  - [ ] **后端：文档投放 API**
    - [ ] `POST /api/v1/admin/documents/upload` —— 接收多文件上传（PDF/DOCX/TXT/MD）
    - [ ] 文件落盘到 MinIO `raw-documents` 桶，DB 写 `documents` 表
    - [ ] 调用 **PDF 解析 v2** 产出 `ParsedDocument`
    - [ ] **触发 RAG 通道**：`ParsedDocument` → splitter → batch_embed → Milvus `chunks` 写入
    - [ ] **触发 GraphRAG 通道**：`ParsedDocument.text` 落到 `input/` → `graphrag index` → PG `entities/relationships` + Milvus `entity_embeddings`
    - [ ] 两路并行同步跑（本期不上 Celery），返回 `{doc_id, rag_status, graph_status, rag_chunks, graph_entities, graph_rels}`
  - [ ] **后端：进度反馈**
    - [ ] 任务表 `ingestion_tasks`（id / doc_ids / status / rag_progress / graph_progress / error_log）
    - [ ] `GET /api/v1/admin/ingestion/status/{task_id}` 轮询进度
    - [ ] **本期同步实现**：上传接口同步返回两路结果；下期再拆异步
  - [ ] **前端：管理员投放页**（`/admin/ingest`）
    - [ ] 拖拽上传组件
    - [ ] 历史任务列表（已上传 / 处理中 / 完成 / 失败）
    - [ ] 任务详情抽屉：RAG 通道 chunks 数 / GraphRAG 通道 新增实体+关系数 / 错误日志
  - [ ] **权限控制**
    - [ ] DB 加 `users.role` 字段
    - [ ] JWT 携带 role，前端按 role 隐藏入口
    - [ ] 后端 `require_admin` 依赖
  - [ ] **审计与可观测**
    - [ ] `audit_logs` 表（谁/何时/上传了哪些文件/两路分别产出多少）
    - [ ] 关键指标：任务耗时 / 智谱 API 调用次数 / Token 用量 / 两路新增数

---

- [ ] **LLM 意图路由**（双路融合的"调度器"，阶段三实现，本期在清单里先占位）
  - [ ] 设计路由 prompt：判断用户问题是 **"查事实"** 还是 **"找关系/做总结"**
  - [ ] 路由决策表：
    - 查事实类 → RAG 通道（Milvus `chunks` top-k）
    - 关系/总结类 → GraphRAG 通道（先 PG 查子图，再 Milvus 取实体描述）
  - [ ] 实现 `route_query(question: str) -> {"channel": "rag"|"graphrag", "params": ...}`
  - [ ] 输出统一封装：返回 `{answer, sources: [{type, ref, snippet}], channel_used}`

---

## 🤖 阶段三：Agent核心功能开发（第6-9天）

### 3.1 问答服务开发

#### 优先级 P0 - 必须完成

- [ ] **Agent框架搭建**
  - [ ] 搭建 LangChain/LlamaIndex 框架
  - [ ] 配置 LLM 接口
  - [ ] 实现基础对话链

- [ ] **检索模块开发**
  - [ ] 实现向量检索（Milvus `chunks` + `entity_embeddings`）
  - [ ] 实现关键词检索
  - [ ] 实现检索结果重排序

- [ ] **LLM 意图路由**（双路融合的"调度器"）
  - [ ] 设计路由 prompt：判断用户问题是 **"查事实"** 还是 **"找关系/做总结"**
  - [ ] 路由决策表：
    - 查事实类 → RAG 通道（Milvus `chunks` top-k）
    - 关系/总结类 → GraphRAG 通道（先 PG 查子图，再 Milvus 取实体描述）
  - [ ] 实现 `route_query(question: str) -> {"channel": "rag"|"graphrag", "params": ...}`
  - [ ] 输出统一封装：返回 `{answer, sources: [{type, ref, snippet}], channel_used}`

- [ ] **双路检索融合**
  - [ ] RAG 通道：Milvus `chunks` 向量召回 → 智谱 rerank → 构造上下文
  - [ ] GraphRAG 通道：实体 mention 抽取 → PG 取 1-2 跳子图 → Milvus `entity_embeddings` 取描述补全 → 社区摘要
  - [ ] 通道封装成 `Retriever` 接口（`retrieve(question) -> List[Doc]`），路由后只调对应实现
  - [ ] 来源标注：每个 `Doc` 带 `{channel, doc_id, page, snippet, score}`
  - [ ] 通道对比评测集：5 个查事实题 + 5 个关系/总结题，分别跑两路，标注胜出方

- [ ] **Prompt工程**
  - [ ] 设计系统提示词
  - [ ] 设计上下文模板
  - [ ] 设计回答格式模板
  - [ ] Prompt 调优

- [ ] **问答API开发**
  - [ ] POST /api/v1/chat 接口
  - [ ] 会话管理
  - [ ] 多轮对话支持
  - [ ] 流式输出支持

---

### 3.2 图片检索服务

#### 优先级 P0 - 必须完成

- [ ] **图片检索接口**
  - [ ] GET /api/v1/images/search 接口
  - [ ] 文本搜图功能
  - [ ] 标签过滤功能
  - [ ] 分页返回

- [ ] **上下文图片推荐**
  - [ ] 分析问答内容
  - [ ] 提取图片需求关键词
  - [ ] 自动关联相关图片
  - [ ] 图片与回答匹配展示

---

### 3.3 PPT生成服务

#### 优先级 P1 - 重要

- [ ] **PPT模板设计**
  - [ ] 设计教学模板
  - [ ] 设计报告模板
  - [ ] 设计总结模板
  - [ ] 模板样式统一

- [ ] **内容规划模块**
  - [ ] 主题分析
  - [ ] 大纲生成
  - [ ] 知识点分配
  - [ ] 内容结构化

- [ ] **PPT生成引擎**
  - [ ] 集成 python-pptx
  - [ ] 实现封面页生成
  - [ ] 实现目录页生成
  - [ ] 实现内容页生成
  - [ ] 实现图片插入
  - [ ] 实现参考来源页

- [ ] **PPT API开发**
  - [ ] POST /api/v1/ppt/generate 接口
  - [ ] GET /api/v1/ppt/status/{id} 接口
  - [ ] GET /api/v1/ppt/download/{id} 接口
  - [ ] 异步任务队列

---

## 💻 阶段四：前端开发（第8-11天）

### 4.1 基础框架搭建

- [ ] **项目初始化**
  - [ ] 创建 React 项目
  - [ ] 配置 TypeScript
  - [ ] 配置 Tailwind CSS
  - [ ] 配置路由
  - [ ] 配置状态管理

- [ ] **UI组件库**
  - [ ] 选择组件库（Ant Design / shadcn/ui）
  - [ ] 设计整体布局
  - [ ] 设计主题配色
  - [ ] 封装通用组件

---

### 4.2 核心页面开发

#### 优先级 P0 - 必须完成

- [ ] **问答页面**
  - [ ] 对话界面设计
  - [ ] 消息气泡组件
  - [ ] 输入框组件
  - [ ] 来源标注展示
  - [ ] 图片卡片展示
  - [ ] 相关问题推荐

- [ ] **图片展示组件**
  - [ ] 图片网格布局
  - [ ] 图片预览弹窗
  - [ ] 图片信息展示
  - [ ] 图片下载功能

---

#### 优先级 P1 - 重要

- [ ] **PPT预览页面**
  - [ ] PPT缩略图展示
  - [ ] PPT预览播放
  - [ ] PPT下载功能

- [ ] **用户中心**
  - [ ] 登录注册页面
  - [ ] 个人信息页面
  - [ ] 问答历史页面
  - [ ] 收藏夹页面

---

### 4.3 响应式适配

- [ ] **移动端适配**
  - [ ] 响应式布局调整
  - [ ] 移动端导航
  - [ ] 触摸交互优化

---

## 🔐 阶段五：用户系统开发（第10-12天）

### 5.1 认证系统

#### 优先级 P1 - 重要

- [ ] **用户注册登录**
  - [ ] 学号认证接口
  - [ ] JWT Token 生成
  - [ ] 登录状态管理
  - [ ] 密码加密存储

- [ ] **权限控制**
  - [ ] RBAC 权限模型
  - [ ] API 权限拦截
  - [ ] 前端路由守卫

---

### 5.2 用户数据管理

- [ ] **历史记录**
  - [ ] 问答历史存储
  - [ ] 历史记录查询
  - [ ] 历史记录删除

- [ ] **收藏功能**
  - [ ] 问答收藏
  - [ ] 图片收藏
  - [ ] 收藏夹管理

---

## 🚀 阶段六：测试与优化（第13-14天）

### 6.1 测试

- [ ] **单元测试**
  - [ ] 核心业务逻辑测试
  - [ ] 工具函数测试
  - [ ] 测试覆盖率 > 80%

- [ ] **集成测试**
  - [ ] API 接口测试
  - [ ] 数据库操作测试
  - [ ] 第三方服务 Mock

- [ ] **端到端测试**
  - [ ] 问答流程测试
  - [ ] PPT生成流程测试
  - [ ] 用户注册登录测试

---

### 6.2 性能优化

- [ ] **后端优化**
  - [ ] 接口响应时间优化
  - [ ] 数据库查询优化
  - [ ] 缓存策略实施
  - [ ] 并发性能测试

- [ ] **前端优化**
  - [ ] 首屏加载优化
  - [ ] 代码分割
  - [ ] 图片懒加载
  - [ ] 资源压缩

---

### 6.3 质量保证

- [ ] **代码审查**
  - [ ] 代码规范检查
  - [ ] 安全漏洞扫描
  - [ ] 代码注释完善

- [ ] **用户测试**
  - [ ] 内部用户测试
  - [ ] 收集用户反馈
  - [ ] 问题修复

---

## 🏭 阶段七：部署上线（第15-16天）

### 7.1 部署准备

- [ ] **生产环境配置**
  - [ ] 服务器资源规划
  - [ ] 域名和SSL证书
  - [ ] 环境变量配置
  - [ ] 数据库备份策略

- [ ] **Docker镜像**
  - [ ] 前端镜像构建
  - [ ] 后端镜像构建
  - [ ] 镜像推送到仓库

---

### 7.2 正式部署

- [ ] **服务部署**
  - [ ] 部署数据库服务
  - [ ] 部署后端API服务
  - [ ] 部署前端静态服务
  - [ ] 配置Nginx反向代理

- [ ] **监控配置**
  - [ ] 部署监控系统
  - [ ] 配置告警规则
  - [ ] 日志收集配置

---

### 7.3 上线验收

- [ ] **功能验收**
  - [ ] 问答功能验收
  - [ ] 图片检索验收
  - [ ] PPT生成验收
  - [ ] 用户系统验收

- [ ] **性能验收**
  - [ ] 响应时间达标
  - [ ] 并发能力达标
  - [ ] 稳定性测试通过

---

## 📊 任务统计

### 按优先级统计

| 优先级     | 任务数量 | 说明               |
| ---------- | -------- | ------------------ |
| P0（必须） | 45       | 核心功能，必须完成 |
| P1（重要） | 25       | 重要功能，应完成   |
| P2（可选） | 10       | 增强功能，可延后   |

### 按阶段统计

| 阶段       | 周数    | 任务数量 |
| ---------- | ------- | -------- |
| 项目准备   | 1-2周   | 20       |
| 知识库构建 | 3-5周   | 25       |
| Agent开发  | 6-9周   | 30       |
| 前端开发   | 8-11周  | 20       |
| 用户系统   | 10-12周 | 10       |
| 测试优化   | 13-14周 | 15       |
| 部署上线   | 15-16周 | 15       |

---

## 📌 关键里程碑

| 里程碑         | 目标日期 | 交付内容                     |
| -------------- | -------- | ---------------------------- |
| M1: 知识库可用 | 第5周末  | 知识库数据导入完成，检索可用 |
| M2: MVP版本    | 第8周末  | 基础问答功能上线             |
| M3: V1.0版本   | 第12周末 | 图片检索+PPT生成功能上线     |
| M4: 正式上线   | 第16周末 | 完整功能上线，开放用户使用   |

---

## 📝 任务执行规范

### 任务状态说明

| 状态   | 标记 | 说明         |
| ------ | ---- | ------------ |
| 未开始 | [ ]  | 任务待执行   |
| 进行中 | [~]  | 任务执行中   |
| 已完成 | [x]  | 任务已完成   |
| 已阻塞 | [!]  | 任务遇到障碍 |
| 已取消 | [-]  | 任务不再需要 |

### 每日更新

- 每日更新任务状态
- 记录遇到的问题
- 调整优先级（如需）

---

## 🔔 风险跟踪

| 风险           | 可能性 | 影响 | 应对措施     | 状态   |
| -------------- | ------ | ---- | ------------ | ------ |
| 知识库版权问题 | 高     | 高   | 仅限校内使用 | 监控中 |
| LLM回答准确性  | 中     | 高   | 多重验证机制 | 监控中 |
| 开发进度延期   | 中     | 中   | 预留缓冲时间 | 监控中 |
| 用户接受度低   | 低     | 中   | 用户调研     | 监控中 |

---

**文档维护**：本任务清单将随项目进展持续更新，建议每日检查并更新任务状态。
